<h1>Text Shadow on Mouse Move event</h1>

We got the mouse location by using offsetWidth and offsetHeight

```javascript
const width = hero.offsetWidth;
    const height = hero.offsetHeight;

    let { offsetX: x, offsetY: y } = e;
 ```
 
 
